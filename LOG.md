# LOG — 履歴

> このファイルは**追記オンリー**。完了したPhaseや解決した問題を、新しいものを上に積んでいく。
> 過去の記述は書き換えない（間違っていたこともそのまま記録として残す）。

---

## 完了済みPhase

| Phase | 内容 |
|-------|------|
| LeftHand 常時表示化（2026-05-29） | メタクエストで左手 Ray Interactor を無効化（Experiment.md §4.3 方針）した副作用でパネルのボタンが押せなくなった件への対応。①Ray Interactor の再有効化は Unity Editor 側で実施（コード変更なし）。②`HandVisualizer.cs`：仮想左手を常時表示に変更。`Start()` の `virtualHandRoot.SetActive(false)` を `true` に、`HandleStateChanged` 内の `virtualHandRoot.SetActive(ShouldShowHand(state))` を削除（休憩中・タスク外でも LeftHand を表示するユーザー要望）。`ShouldShowHand` メソッドは未使用になるが将来用に残置（コメント明記）。`delayMs` リセット処理は維持 |
| 解析パイプライン段階1: 前処理+エポック切り出し（2026-05-29） | LSL記録の `.xdf`（Delsys EMG 16ch/1000Hz + UnityMarkers 89イベント）の解析環境を構築。EMG PC で marker_monitor.py 実行成功（`.py`直打ち→VS Code起動でWin7非対応エラーが出ていたが `python <path>` で解決）。`tools/plot_emg_markers.py` 新規作成：xdfからEMG波形+マーカーを共通LSL時刻軸で重ねてプロット（ch引数指定・イベント種別色分け・時間ズーム・y軸自動スケール・画像保存）。`tools/emg_preprocess_epochs.py` 新規作成：バンドパス(20-450Hz,filtfilt)→全波整流→RMS包絡線(50ms)→マーカー照合→エポック切り出し→.npz保存。TaskA基準=MotionOnset_A_{finger}（直前のAutoMotionStartからcond/trial/finger取得）窓-0.5〜+1.0s、TaskB基準=FlexionDetected_B_{trial}_count{k} 窓-1.0〜+0.5s（Δt/SoA/conditionをメタ付与）。確認プロット（平均RMS±SEM、overflow窓/運動準備窓ハイライト）対応。`tools/README.md` に両スクリプトの使い方追記。**所見：記録EMGで明確な信号はch1のみ（2ch目未検出＝Delsysセンサー2個目未装着の可能性）。TaskA後半に大きなEMG活動（被験者が動いた可能性、安静維持の徹底要）。テストデータはtrial19から記録されておりTaskB conditionがunknown＝本計測はブロック先頭から記録すること。** |
| VRテスト後修正 D-4, B-10（2026-05-26） | ①D-4: `TaskAController`/`TaskBController` の `HandleStateChanged` に `lastHandledState` フィールドを追加し、同一ステートが連続発火した場合は早期 return する保護を追加（OnStateChanged 多重発火対策）。**ただし、シーン内に同コンポーネントが複数アタッチされている場合は各インスタンス独立なので根本対応にはならず、Unity Editor 側で重複削除が必要**（STATUS.md に最優先確認事項として追記）。②B-10: TaskBController の QUEST 法（ベイズ推定型 PDF）を Staircase 法に書き換え。`questPdf` 配列と `MaxDelayMs` 定数を削除し、`currentStaircaseDelta` (float) に置換。SerializeField: `staircaseInitialDelta=0f`/`staircaseStepUp=50f`/`staircaseStepDown=30f`/`staircaseMinDelta=0f`/`staircaseMaxDelta=600f`。挙動：No 応答→ currentStaircaseDelta += 50ms、Yes 応答→ -30ms、初期 0ms、範囲 [0,600]ms にクランプ。`InitializeQuest`/`QuestMean`/`QuestUpdate` のメソッド名は呼び出し側互換のため維持 |
| VRテスト後修正 D-3, B-9, M-1（2026-05-26） | ①D-3: `TaskAController` / `TaskBController` に `taskAMainCoroutine` / `taskBMainCoroutine` / `taskBPracticeCoroutine` フィールドを追加。`HandleStateChanged` で前回のコルーチンを `StopCoroutine` してから新規 `StartCoroutine`。`AbortTask` でも参照クリア。これでスキップ→次ブロックでコルーチンが多重起動して残った試行が次ブロックに混入する問題を解消。②B-9: SoA Yes/No ログを Trial 番号付きで統一。Yes 時にも `Debug.Log("[Task B] Trial N: Yes (Δt=...ms)")` を出力。No 時も `Trial N: No (no response, treated as No) (Δt=...ms)` 形式に統一。Practice 側も同様。③M-1: `AdvanceState` に `case Finished: ShowStartMenu()` 追加。`GetNextState(Finished) = StartMenu` 追加。`SkipCurrentPhase` で StartMenu/TestMenu/ExperimentMenu 行きの場合は `SwitchState` 経由（初期化処理を確実に実行） |
| クリーンアップ: taskBCompletedFlag 廃止（2026-05-26） | D-1 で taskBCompletedFlag を使ったロジックを DetermineNextStateAfterBlockRest に置き換えた際、フィールド代入のみ残って読み取りがなくなっていた（CS0414 警告）。フィールド宣言・Start/SkipCurrentPhase/GoBackPhase/SwitchState/NotifyTaskBCompleted の全代入を削除し、Obsolete だった NotifyTaskBCompleted メソッド自体も削除（外部呼び出しなし確認済み） |
| VRテスト後修正 D-2, B-8（2026-05-26） | ①D-2: `SkipCurrentPhase` で TaskA_Main/TaskB_Main をスキップ時、Controller の `MarkCurrentBlockExcluded()` を呼んで `CompletedBlocks` をインクリメントするロジック追加。`TaskBController` に `MarkCurrentBlockExcluded()` 新規追加（旧 `TaskAController.MarkCurrentBlockExcluded` と同等）。これで TaskA(async) の Next ボタン押下後 Finished に飛ぶバグ修正（旧: doneA==0 でフォールバックの Finished に行っていた）。`DetermineNextStateAfterBlockRest` のフォールバック条件も `<= 1` に緩和（スキップ未処理時の安全側遷移）。②B-8: `TaskBController` に `ResponseWindowSeconds` public プロパティ追加。`SoAResponseUI.Start()` で `taskBController.ResponseWindowSeconds` を取得して同期。Inspector 二重管理を解消（旧: 別 SerializeField で 3秒/5秒が混在するリスクあり） |
| VRテスト後修正 B-6, B-7, A-5, D-1（2026-05-26） | ①B-6: `TaskBController` に `CompletedBlocks`/`TotalTrialsPerBlock`/`PracticeTrialCount` public プロパティ追加。`ParticipantHUD.HandleStateChanged` で ステート（Practice/TaskB_Main）に応じて `totalTrialsForDisplay` を動的設定（旧: ハードコード 55）。②B-7: `TaskBController` に `taskBPanelMessageText`/`preMainNoticeSeconds=5f`/`preMainNoticeMessage` SerializeField 追加。`TaskBMainRoutine` 冒頭で「これから本番です」を5秒表示してから試行ループ開始。LSL マーカー `PreMainNotice_B_{condition}` も送出。③A-5: TaskA 指屈曲が起こらない問題の調査用に詳細デバッグログ追加。`TaskAController.TaskAMainRoutine` で `handVisualizer` null チェック + `StartAutoMotion` 呼び出し前後で `Debug.Log`。`HandVisualizer.AutoMotionRoutine` で各 MCP/PIP/DIP の null 詳細表示 + 開始ログ。④D-1: 実験順序を **TaskB(async)→TaskA(async)→VHI誘導B→TaskB(sync)→VHI誘導A→TaskA(sync)** に変更。`ExperimentManager.AdvanceState` から条件分岐を `DetermineNextStateAfterBlockRest()` に切り出し、`lastStateBeforeBlockRest` と `taskBController.CompletedBlocks`/`taskAController.CompletedBlocks` の組み合わせで次ステートを決定。`GetNextState` の TaskA_Main→Finished を TaskA_Main→BlockRest に変更（async完了直後の BlockRest 経路を確保）。`GetPreviousState` も同期更新。`TaskAController` に `CompletedBlocks` public プロパティ追加。`TaskBController.TaskBMainRoutine` 末尾の `NotifyTaskBCompleted()` 呼び出しを `ChangeState(BlockRest)` 直呼びに変更。`ExperimentManager.NotifyTaskBCompleted` を `[Obsolete]` 化（互換性維持のため残置）。`Experiment.md §7` 全体フローを新順序に書き換え |
| VRテスト後修正 B-5 / A-3 / A-4（2026-05-26） | ①B-5: `TaskBController.flexionCountPerTrial` を 5→3 に変更（1試行あたりの屈曲回数削減）。Experiment.md の試行構成・所要時間記述も同期。実験順序（Practice → TaskB/async → TaskB/sync → TaskA/async → TaskA/sync）は v5.3 Phase D 実装で既に成立済み。②A-3: `TaskAController` に屈曲指の Debug.Log（`[Task A] Trial X/21 (async): Flexing IndexFingerFlexion` 形式）を試行開始時に追加。マイルストーン通知用イベント `OnProgressMilestone(int remainingPercent, float remainingSeconds)` 追加。各試行終了時に `CheckProgressMilestone()` で残り 75/50/25/10% を初回到達検出し、`ProgressMilestone_A_{condition}_RemainingNpct` マーカー送出 + イベント発火。残り秒数は (残り試行数 × (autoMotionDuration + (intervalMin+intervalMax)/2)) で概算。③A-4: `TaskAHUD.cs` を全面書き換え。試行進捗・指・カウントダウン表示を削除し、マイルストーン通知専用に変更。普段は空白、マイルストーン到達時のみ `notificationDuration=3秒` 間「あと約 N 分です」を `milestoneText` に表示 + 660Hz サイン波ビープ音（`AudioClip.Create` で実行時生成）。残り60秒以上は分単位、60秒未満は10秒単位で四捨五入 |
| VRテスト後修正 B-1〜B-4, A-1, A-2（2026-05-26） | ①B-1: `TaskBPracticeRoutine` 末尾に `postPracticeDelaySeconds=3.0f` 待機を追加。Practice_Panel で練習5回×3試行→3秒待機→`AdvanceState()` で TaskB_Panel (TaskB_Main async) へ遷移。②B-2: `questTrialsCount=35→15`、`totalTrials=55→23`。`GenerateFixedTrials()` の `for(i<5)→for(i<2)` で固定試行を 4Δt×2 = 8試行に変更（合計 1ブロック 23試行、async+sync で 46試行）。③B-3: 新規 SerializeField `postFlexionDelaySeconds=3.0f` を追加。`TaskBMainRoutine` と `TaskBPracticeRoutine` 両方で計測フェーズ完了→回答フェーズ開始の間に 3秒待機。④B-4: `OnHandSignDetectedHandler` と `SubmitSoAResponse` に Debug.Log 追加（呼び出し順序と previous currentSoAResponse をログ）。`ExperimentUI.cs` のキーボード Y/N にも Debug.Log 追加。⑤A-1: `Assets/Scripts/UI/TaskAHUD.cs` を新規作成。`TaskA_Main` 中のみ表示、試行進捗・現在の指（人差し指/中指/薬指）・次屈曲までの残り秒数を表示。`TaskAController` に `OnTrialStartCue(int,int)/OnAutoMotionStart(AutoMotionType)/OnAutoMotionEnd/OnIntervalCountdown(float)` イベント + `TotalTrialsPerBlock` プロパティ追加。⑥A-2: `HandVisualizer.AutoMotionType` に `MiddleFingerFlexion`/`RingFingerFlexion` を追加。`middleMCP/PIP/DIP`, `ringMCP/PIP/DIP` SerializeField 追加。`AutoMotionRoutine` を motionType で屈曲対象切替に書き換え。`ResetAutoMotionState` を3指分の復帰に拡張。`TaskAController.trialsPerBlock=20→21`、`autoMotionInterval` を `autoMotionIntervalMin=5f`/`autoMotionIntervalMax=10f` に分割。`TaskAMainRoutine` に `GenerateMotionSequence()` を追加（各指7回ずつ→Fisher-Yates シャッフル）。試行間待機を `Random.Range(min,max)` でランダム化＋毎フレーム `OnIntervalCountdown` 発火 |
| Phase G + VRデバッグ Bug Fix 2（2026-05-25） | ①`TaskBController.cs`：`practiceTrialCount=3` SerializeField 追加。`HandleStateChanged` に `Practice` ステートのハンドリング追加（`TaskBPracticeRoutine()` 起動）。`TaskBPracticeRoutine()` を新規追加：`practiceTrialCount` 回の試行（Δt=0ms固定、QUEST更新なし）、条件="practice" で既存 CSV に記録、LSLマーカー（`PracticeStart_B`/`TrialStart_B_Practice_{n}_Delta0ms`/`FlexionDetected_B_Practice_{n}_count{k}`/`ResponseWindowStart_B_Practice_{n}`/`SoA_Yes/No_Practice_{n}_Dt0ms`/`TrialEnd_B_Practice_{n}`/`PracticeEnd_B`）。完了後 `ExperimentManager.Instance.AdvanceState()` で `TaskB_Main`(async) へ自動遷移。Task A 練習は元々コードに存在しないため変更なし。②`ParticipantHUD.cs`：`Start()` 末尾に `HandleStateChanged(CurrentState)` を追加（`taskBPanel` 子配置時の遅延 Start 問題を修正）。`HandleStateChanged` で `Practice` ステートも表示対象に追加（練習中も HUD 表示）。回答指示テキストを「握りこぶし」→「ピンチ（親指+人差し指）」に更新。③`HandSignDetector.cs`：`thumbTip`/`indexTip` null 時の一回限り警告ログ追加。検出成功時の `Debug.Log` 追加。`EnableDetection=true` 中の距離定期ログ（0.5秒間隔）追加（閾値調整用） |
| v5.3 Phase F（2026-05-25） | Task A 時間ベース化。`TaskAController.cs`：`stillnessSpeedThreshold`/`stillnessDuration`/`preMotionWait` SerializeField 削除、`WaitForStillness()` コルーチン削除。新規 SerializeField 追加：`autoMotionStartDelay=5.0f`/`autoMotionDuration=0.5f`/`autoMotionInterval=10.0f`。`TaskAMainRoutine` をブロック先頭5秒待機→試行ループ（屈曲0.5秒→10秒待機）の時間ベース構造に書き換え。`HandVisualizer.cs`：`StartAutoMotion(AutoMotionType, float duration=2.0f)` にシグネチャ拡張。`AutoMotionRoutine` が duration を引数で受け取るよう変更（デフォルト2.0fで TestModeController 互換維持） |
| VRデバッグ対応（2026-05-25） | ①ParticipantHUD / SoAResponseUI に `OnStateChanged` 購読を追加し `TaskB_Main` 中のみ `gameObject.SetActive(true)`（練習・誘導・BlockRest 中の残留表示を修正）。②`ParticipantHUD.HandleFlexionDetected` でローカル屈曲カウンタ（`flexionDetectedCount`）を+1して `屈曲 N/total` 表示更新に変更（旧: ペース合図タイミングで `current-1` 表示していたため最後の屈曲が反映されず 4/5 のまま回答フェーズに入るバグを修正）。③`HandVisualizer.HandleStateChanged` で `TaskB_Main` 以外のステートでは `delayMs=0` に強制リセット（Practice ステート中に前回 delayMs が残るバグを修正）。④`HandSignDetector.cs` を全面書き換え：握りこぶし検出（指先-手首距離平均、閾値0.05m）→ ピンチ（親指+人差し指タッチ、閾値0.025m）に変更。イベント `OnFistDetected` → `OnHandSignDetected`、プロパティ `CurrentAverageDistance` → `CurrentPinchDistance`、メソッド `IsFistDetectedNow()` → `IsHandSignDetectedNow()`。SerializeField `wrist`/`fingerTips[]` → `thumbTip`/`indexTip`。⑤`responseWindowSeconds` を 3 秒 → **5 秒**に変更（TaskBController / SoAResponseUI 両方）。⑥TMP フォント動的化は Unity Editor 側の設定（Atlas Population Mode = Dynamic）で対応予定 |
| v5.3 Phase E3（2026-05-25） | `VASInputUI.cs` → `SoAResponseUI.cs` にリネーム（GUID 保持）。SoA Yes/No ボタンを廃止し、`promptText`/`countdownText`/`fistFeedbackText` の TextMeshPro 表示に変更。`HandSignDetector.OnFistDetected` を購読し「Yes 受付！」フィードバック。3 秒カウントダウンコルーチン。`ParticipantHUD.cs` を新規作成：試行/屈曲進捗表示、円形拍動アニメーション、AudioClip.Create による実行時ビープ音生成（高音=ペース合図、低音=試行開始/回答開始）。TaskBController のイベント（OnTrialStartCue/OnPacingCue/OnFlexionDetected/OnResponseWindowOpened/OnSoAWindowClosed）を購読 |
| v5.3 Phase E2（2026-05-25） | `TaskBController.cs` の `TaskBMainRoutine` を全面書き換え。Onset 待機ループ＋Δt 待機を廃止し、計測フェーズ（ペース化屈曲を `flexionCountPerTrial` 回検出、`pacingInterval` 静止区間で分離）と回答フェーズ（HandSignDetector で握りこぶし検出 = Yes、無反応 = No、`responseWindowSeconds` で時間切れ）に分離。新規 SerializeField（`pacingInterval`/`flexionCountPerTrial`/`responseWindowSeconds`/`handSignDetector`）と新規イベント（`OnTrialStartCue`/`OnPacingCue`/`OnResponseWindowOpened`/`OnFlexionDetected`）追加。新規 LSL マーカー（`FlexionDetected_B`/`ResponseWindowStart_B`/`SoA_Yes_Trial`/`SoA_No_Trial`）。廃止マーカー（`MotionOnset_B`/`SoAResponse_{0,1}`/`SoAResponse_Missed`）。CSV ログに `condition`/`flexion_count`/`response_time` カラム追加、`motion_onset_time` カラム削除。`HandVisualizer.cs` に `FlexionDetectionMode`（VelocityBased / AngleVelocityBased）切替機構を追加 |
| v5.3 Phase E1（2026-05-25） | `Assets/Scripts/Common/HandSignDetector.cs` を新規作成。指先 Transform 配列と手首 Transform の距離平均ベースで握りこぶし（Fist）を検出する独立コンポーネント。`EnableDetection` フラグで回答フェーズのみ有効化、`detectionCooldown` で連続発火防止、`OnFistDetected` イベントを発火、`CurrentAverageDistance` でデバッグ可視化 |
| v5.3 Phase D（2026-05-25） | async/sync を「VHI 誘導の有無」で再定義。`VHIInductionController.cs:64` の `delayMs=500` 設定を `delayMs=0` 固定に変更（旧 v5.2 の async=Δt=500ms 廃止）。`TaskBController.cs` に async/sync の 2 ブロック構造を追加（`currentBlockIndex`/`CurrentCondition`/`HasRemainingBlocks`/`CompleteCurrentBlock`）、QUEST をブロック毎再初期化。`TaskAController.cs` の `CurrentCondition` を `"async"→"sync"` に順序反転。`BlockStart_B_{condition}` / `BlockEnd_B_{condition}` / `BlockStart_A_{condition}` / `BlockEnd_A_{condition}` マーカー追加。`ExperimentManager.AdvanceState` の Practice → `TaskB_Main` 直行、BlockRest 分岐を condition 判定に再編。`GetNextState`/`GetPreviousState` も同期更新。`LSL_MARKERS.md` 更新 |
| LSL マーカー補完 + 監視スクリプト（2026-05-25） | `ExpStart`/`PracticeEnd`/`RestEnd`/`TaskB_Start`/`TaskB_End`/`TaskA_Start`/`TaskA_End`/`AutoMotionStart_A_{condition}_{n}` の 8 種マーカーを追加。`tools/marker_monitor.py`（pylsl 監視スクリプト）と `tools/README.md` を新規作成。`LSL_MARKERS.md` を新規作成し全マーカーをドキュメント化 |
| Phase A.5 補正（2026-05-25） | `ExperimentManager.cs` に `lastStateBeforeBlockRest` を追加し、`ChangeState` で TaskA_Main/TaskB_Main からの BlockRest 流入時のみ記録。`GetPreviousState` の BlockRest 分岐を `lastStateBeforeBlockRest` 参照に変更し、GoBackPhase 経由（TaskA_Induction → BlockRest 等）での経路破壊を防止。`GetNextState` の TaskA_Main → `BlockRest` を `Finished` に変更（Next ボタンで無条件 Finished へ）。Start/SwitchState でメニュー復帰時に lastStateBeforeBlockRest をリセット |
| LSL ファイル整理（2026-05-25） | `Scripts/LSL/` の未使用 3 ファイル（`LslClockSynchronizer.cs` / `LslHealthMonitor.cs` / `EmgLslInletReceiver.cs`）を削除。理由：どこからも参照されておらず、EMG は EMG PC で記録する設計のため Unity 側 Inlet 不要。残置：`IMarkerSender.cs` / `LslMarkerSender.cs` / `DebugMarkerSender.cs` / `MarkerSenderRouter.cs` の 4 ファイル |
| v5.3 Phase B（2026-05-22） | VAS の全廃。`Data/VASRecorder.cs` 削除、`UI/VASInputUI.cs` から VAS 関連 SerializeField/メソッド削除（SoA 部分は Phase E まで残置）、`ExperimentManager.cs` から `EvaluateTaskAVAS` / `EvaluateTaskBVAS` と `taskARetryCount` / `taskBRetryCount` 削除。`TaskAController.MarkCurrentBlockExcluded` は Phase D 再利用候補としてコメント付き残置 |
| v5.3 Phase A.5（2026-05-22） | Consent/TaskA_VASCheck/TaskB_VASCheck の3ステートを削除。ExperimentManager に SkipCurrentPhase()/GoBackPhase()/GetNextState()/GetPreviousState() を追加。VHIInductionController の Induction 完了後遷移を VASCheck → Baseline に短絡。VASInputUI の HandleStateChanged から VASCheck 分岐削除（VAS 本体は Phase B で削除予定）。consentPanel SerializeField 削除 |
| v5.3 Phase A（2026-05-22） | タスク順序を Task B → Task A に反転。ExperimentManager に taskBCompletedFlag / NotifyTaskBCompleted() を追加し、AdvanceState の Practice/BlockRest 分岐と EvaluateTaskBVAS 除外時遷移を反転。TaskBController:176 の完了時 ChangeState(Finished) → NotifyTaskBCompleted() に変更 |
| P3 Task Aフロー | 静止確認（<5cm/s・1秒継続）・10秒待機→屈曲サイクル・onset検出のTask B専用化 |
| P2 人差し指屈曲 | AutoMotionType→IndexFingerFlexion・MCP/PIP/DIP 30°屈曲（LateUpdate差分加算）・Inspectorスロット追加 |
| P1 構造修正 | VirtualLeftHand分離・ApplyDelayedPose座標系修正（world統一）・_skeletonDriverインフラ全削除 |

## 解決済みの問題（〜2026-05-18）

| # | 問題 | 解決 |
|---|------|------|
| 1 | virtualHandWristのworld-position直接代入 | P1-3で解決 |
| 2 | 親指付け根のボーン変形（world position書き込み） | P1-4で解決 |
| 3 | XRHandSkeletonDriverとのUpdate競合 | P1-5（分離）で解決 |
