# プロンプト：VHI実験 EMGグラフ作成（正規化・チャネル加減算）

> 別チャットにそのまま貼って使うための指示書。データ・前処理・正規化・図の構成・確認手順まで自己完結で記述している。

---

以下の指示に従って、VHI実験のEMGグラフを作成してください。**作図の前に、必ず「どんな図をどう作るか」を提示して私の確認を取ってから**進めてください。作図後は**各グラフの作成方法（正規化式・集計・時間窓）を説明**してください。

## 1. データと前処理

- 左前腕 sEMG 4ch（1000Hz）＋Unityマーカーを LSL 記録した XDF が `実験データ/{被験者}/{被験者}.xdf` にある。
- 既存の解析スクリプトが `VHI実験/scripts/` にあり、`extract.py` を実行すると全被験者のエポックを `epochs_all.npz` に書き出す。**まずこれを再利用（無ければ `extract.py` を実行して生成）**してエポックを得ること。前処理・エポック仕様は変更しない。
- **前処理（extract.py 準拠）**：バンドパス 20–450Hz → ノッチ 50Hz(Q=30) → 全波整流 → 5Hz ローパス（線形包絡）。すべて `filtfilt`（ゼロ位相）。解析はこの包絡を使う。
- **チャネル定義**：**ch0・ch1 = 伸筋（extensor／橈側）、ch2・ch3 = 屈筋（flexor／尺側）**。
- **エポック仕様**：
  - **Task A**：`AutoMotionStart`（自動屈曲onset）基準、時間窓 **−0.5〜+1.0s（1500点）**。不随意（overflow）窓 **0–100ms**。
  - **Task B**：`FlexionDetected`（屈曲検出onset）基準、時間窓 **−1.0〜+0.5s（1500点）**。運動準備窓 **−500〜0ms**。
  - `epochs_all.npz` のキー：`{subj}__taskA_raw`（形状 n×4×1500 の包絡）、`{subj}__taskA_cond`（'async'/'sync'）、同様に `taskB_raw` / `taskB_cond`。

## 2. 対象被験者（重要）

- **Task A：takeuchi・sano・hayashi の 3名**（kawamoto は TaskA-sync の VHI 誘導失敗＝約40分待機のため **除外**）。
- **Task B：takeuchi・sano・hayashi・kawamoto の 4名**。
- 集計は「**各被験者で試行を平均 → 被験者間で平均（総加算平均）**」。陰影帯は**被験者間の ±SEM**。

## 3. 正規化（z スコア版・ピーク正規化版の両方を作る）

**raw のグラフは不要。** チャネル間で足し引きするため、各chのスケールを揃える。手順：

1. **被験者ごと・チャネルごと**に正規化する（振幅の大きい被験者が支配しないように、被験者内で正規化してから被験者平均する）。
2. 正規化の基準統計は、**そのタスクの async+sync 全エポック・全時刻を pooled** にして算出（async と sync を同一基準にするため）。
3. 2種類を作る：
   - **z スコア版**：`(x − 平均) / SD`。安静≈0、単位＝SD。→ 引き算（例 2−0）が最もフェア。
   - **ピーク正規化版**：`x / そのchのピーク`（総加算平均の最大値）。0〜1 目安。※安静オフセットが残るため、引き算系の合成は「ピーク比の差」と解釈すること。
4. **合成チャネルは、正規化後のchを足し引きして計算**する（下記）。

## 4. 図の構成（単ch図と合成ch図を分ける）

各タスクで **async と sync を重ねて**表示（TaskA と TaskB は時間軸が違うので別図）。各図内は**同一正規化なのでy軸を揃える**。onset に縦線、評価窓（TaskA:0–100ms／TaskB:−500〜0ms）を薄く塗る。

### (A) 単ch図（4行：ch0, ch1, ch2, ch3）
- Task A：1枚、Task B：1枚。
- これを z版・ピーク版で作る → **計4枚**。

### (B) 合成ch図（10行：以下の10信号）
以下を**正規化後のchで**計算：
1. `2+3−(0+1)`（屈筋 − 伸筋＝正味の屈曲ドライブ）
2. `2+3+0+1`（全ch合計＝総活動）
3. `2−0`
4. `2−1`
5. `2+0`
6. `2+1`
7. `3−0`
8. `3−1`
9. `3+0`
10. `3+1`
- Task A：1枚、Task B：1枚。
- これを z版・ピーク版で作る → **計4枚**。

（合計 8枚：単ch z/ピーク × TaskA/TaskB＝4、合成ch z/ピーク × TaskA/TaskB＝4）

## 5. 進め方

1. まず、**作成予定の図（枚数・各図の中身・正規化方式・対象被験者・集計方法）を箇条書きで提示し、私の承認を得る**。
2. 承認後に作図。各図は日本語ラベルで、凡例に async/sync と n を明記。
3. 作図後、**各グラフの作成方法（正規化の式、被験者内平均→被験者間平均の順、時間窓、合成chの計算）を説明**する。

## 6. ファイル命名と保存先（Obsidian vault）

- **ファイル名は内容がひと目で分かる命名**にすること。「タスク・単ch/合成ch・正規化方式」が分かる形にする。例：
  - `TaskA_単ch_zスコア.png` / `TaskA_単ch_ピーク正規化.png`
  - `TaskB_単ch_zスコア.png` / `TaskB_単ch_ピーク正規化.png`
  - `TaskA_合成ch_zスコア.png` / `TaskA_合成ch_ピーク正規化.png`
  - `TaskB_合成ch_zスコア.png` / `TaskB_合成ch_ピーク正規化.png`
- **図（PNG）と説明を、Obsidian vault `C:\Users\koike\Downloads\VHI` に保存して整理**すること。
  - 例：vault内に `EMGグラフ/` のようなフォルダを作り、そこにPNGを保存。
  - 併せて**まとめノート（Markdown）を1つ作成**し、Obsidian書式（YAML frontmatter、見出し、`![[画像名.png]]` での画像埋め込み、必要なら `[[リンク]]`）で、**各グラフの作成方法（正規化式・被験者内平均→被験者間平均・時間窓・合成chの計算）と結果の要点**を記載する。
  - 図・ノートともに、後から見て何の図か分かるように整理する。

## 7. 補足（任意）

- 可能なら、評価窓（TaskA:0–100ms／TaskB:−500〜0ms）での **async vs sync の要約値（平均±SEM）と検定**も表で添えると良い。
- 正規化の基準（pooled 平均/SD・ピーク）を各図のキャプションに明記すること。
