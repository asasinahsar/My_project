---
tags:
  - 研究計画書
  - 計算論的精神医学
  - EMG
  - VHI
  - 統合失調型
  - motor-overflow
  - 筋シナジー
date: 2026-05-21
version: 5.3
author: 東京科学大学 小池研究室
status: 修正中
---

# バーチャルハンドイリュージョンを用いた自己感覚の計算論的定量化
## ―EMG motor overflow・筋シナジーによるSoO/SoAの客観的測定―

**研究者：** 東京科学大学 小池研究室
**期間：** 2026年5月〜6月（6週間）
**使用機器：** sEMG 2ch・Meta Quest（ハンドトラッキング）・Unity/VR・HMD・LSL同期システム

> **v5.3 改訂概要：** ①Task B のコントロール条件を「VHI誘導の有無」方式に変更（誘導なし＝async / 誘導あり＝sync）。これに伴いタスク順序を **Task B → Task A** に変更。②VAS（主観的品質管理）を全廃。③右手コントローラーをデバイスごと廃止し、操作を**左手に全面移行**。完成版では左手によるUI操作も行わず、タスク失敗時の修正用に「ハンドサインでTask冒頭へ戻す」機能を将来実装する（現時点では未実装）。④`flexionCountPerTrial` 初期値を5に確定。⑤本改訂に伴う研究設計上の限界を 12 節に明記。
>
> **v5.2 からの継続事項：** Task B はペース化された左手首屈曲（QUEST法）＋計測/回答フェーズ分離方式。Onset検出のリングバッファ遅延は廃止し固定遅延方式。Task A は時間ベース待機。主要パラメータは調整可能（巻末のパラメータ表を参照）。

---

## 1. 研究背景と臨床的動機

統合失調症の中核症状である「自分が動かした感覚の喪失（SoA障害）」「身体が自分のものでない感覚（SoO障害）」は、従来、主観的アンケートによってのみ評価されてきた。しかし主観報告は症状の重篤度や病識に依存するため、客観的バイオマーカーとしての信頼性に限界がある。

予測符号化（Predictive Coding）の枠組みでは、これらの症状は脳内の感覚精度重み付け異常——すなわち内部運動予測の精度低下（Π_motor↓）と視覚への過剰な精度付与——として統一的に説明できる。本研究は、この精度重み付け異常を**Meta Questハンドトラッキングによる視覚フィードバック操作**と**2ch表面筋電図（sEMG）による不随意生体反応の定量化**を組み合わせることで客観的に捉えることを目的とする。

### 本研究の独自性

従来のVHI/RHI研究はSoOとSoAを**主観的アンケート（Likertスケール）**で測定してきた。本研究は：

- **SoO**：VHI成立後にバーチャルハンドが動いた際の**不随意的EMG motor overflow**（t=0〜100ms窓）で客観的に定量化
- **SoA**：被験者の随意運動に映像遅延Δtを挿入した際の**運動準備EMGパターンの変容**で客観的に定量化

という、先行研究にない**EMGベースの暗黙的指標**を採用する。さらに2chの高密度sEMGにより**NMF筋シナジー解析**を実施し、VHI条件下での運動制御レベルの変化を初めて定量化する。

> **臨床的意義：** 健常者のSPQスコアを用いた層別化により、精神病傾向の**早期・定量的バイオマーカー開発**に貢献する。

---

## 2. 研究目的

1. **SoO（身体所有感）の客観的定量化**：VHI成立時のEMG motor overflowを指標として、身体所有感の強度をアンケートなしで測定する
2. **SoA（主体感）の客観的定量化**：映像遅延Δtに対する運動準備EMGパターンの変容を指標として、主体感崩壊閾値τ_SoAをEMGで測定する
3. **筋シナジー解析**：2ch EMGのNMF解析により、VHI条件下での筋協調パターン変容を定量化する
4. **SPQとの関連**：得られた指標群と統合失調型特性スコア（SPQ）の相関を検証し、計算論的精神医学モデルへの統合を試みる

---

## 3. 研究仮説

| 指標 | 予測される変化 | 対応する計算論的解釈 |
|------|--------------|-------------------|
| motor overflow量（SoO指標） | **SPQ高群で大きい** | Π_motor↓により視覚→運動の漏れ込みが増大 |
| τ_SoA（主体感崩壊閾値） | **SPQ高群で延長** | 運動予測精度の低下により遅延許容が増大 |
| 筋シナジー変容度 | **SPQ高群で大きい** | 身体表象の可塑性過剰・運動制御の不安定化 |
| sync−async EMG差分 | **SPQ高群で拡大** | VHI感受性の増大 |

---

## 4. 実験デザイン

### 4.1 参加者

- 健常成人 25〜30名
- **右利き者限定**（Edinburgh Handedness Inventory, EHI ≥ +40）
- **非利き手（左手）を実験手として統一**
- 除外基準：神経疾患・精神疾患の既往、上肢運動障害、左利き・両利き（EHI < +40）

> **左手限定の根拠：** 利き手では固有感覚の信頼度が高く身体表象が安定しているため、VHIへの抵抗性が大きい（Dempsey-Jones & Kritikos, 2019）。非利き手の方がmotor overflowを含めたVHI効果が有意に大きく、効果量の確保と被験者間均質性のため採用する。

### 4.2 実験システム構成

```
【スタンドアロン構成（PC不要）】

Meta Quest（単体動作）
├─ ハンドトラッキング：左手の3D関節姿勢をリアルタイム取得
├─ Task A：左手ハンドトラッキングをリアルタイム同期しながら人差し指を自動屈曲（LateUpdate差分加算方式）
├─ Task B：固定遅延（Δt）を挿入して左手バーチャルハンドを描画
└─ UI操作・申告：左手ハンドサイン（右手・コントローラーは不使用）

sEMG（左前腕2ch, 1000Hz以上）
↓ Lab Streaming Layer（LSL）via Wi-Fi
Meta Quest
↓ LSLマーカーで全イベントを共通時刻に記録

オフライン解析（Python）
```

| 機器 | 役割 |
| --- | --- |
| Meta Quest（スタンドアロン） | ハンドトラッキング・VR提示・LSL受信・左手ハンドサイン認識 |
| 左手ハンドトラッキング | Task A 自動運動提示・Task B 遅延ミラーリング・操作/申告のハンドサイン |
| 2ch sEMG | motor overflow・運動準備EMG・筋シナジーの検出 |
| LSL（Wi-Fi経由） | sEMG信号とUnityイベントのサブミリ秒同期 |

> **右手・コントローラー全廃（v5.3）：** 右手コントローラーはデバイスごと使用しない。右手のモデル（コントローラー含む）はVR内に一切表示しない。すべての操作・申告は左手のハンドサインで行う。

> **遅延機構の方針：** Task B の遅延は、Onset検出に基づくリングバッファ遅延を廃止し、各試行に与える固定遅延 Δt を描画タイミングへ直接適用する方式とする。

### 4.3 操作系の設計（左手への全面移行）

すべての操作・申告を左手で行う。ただし**左手はTask BでEMG計測の対象**であるため、操作・申告は必ず計測対象外のタイミング（回答フェーズ・フェーズ間）に限定し、計測中のEMGに操作動作が混入しないようにする。

| 操作対象 | 方法 | タイミング |
|---------|------|-----------|
| 画面遷移・決定 | 左手ハンドサイン | フェーズ間（計測対象外） |
| Task A（受動観察） | 操作なし（静止） | ― |
| Task B SoA崩壊申告「Yes」 | **左手ピンチ（親指+人差し指タッチ）** | 回答フェーズ（計測対象外） |
| Task B SoA崩壊申告「No」 | 応答なし（無反応＝No） | ― |
| タスク失敗時の修正（**将来実装**） | 左手の所定ハンドサインでTask冒頭へ戻す | フェーズ間（計測対象外） |

> **Yes ハンドサイン（v5.3 確定）：** 当初の握りこぶし（Fist）から**ピンチ（親指と人差し指の先端を合わせる）**に変更。理由：ピンチは Meta Quest 標準の検出方式と同等で認識精度が高く、計測フェーズ中の手首屈曲動作との誤検出が少ないため。Meta Quest 標準 PinchSelect との衝突を避けるため、Unity 側で左手 Interactor を実験全体を通じて無効化する（下記参照）。

- レイキャスト（手からのビーム）は**全て非表示**
- 左手 Interactor（Poke / Near-Far / Direct）は実験全体を通じて**無効化**（Meta Quest 標準ピンチ操作との衝突防止）
- 右手・コントローラーモデルは一切表示しない
- 左手はTask中以外は一切表示させない

> **完成版の操作方針（v5.3）：** 完成版では被験者による能動的なUI操作も行わず、実験開始後は自動進行を基本とする。タスク失敗時の救済として「左手の所定ハンドサインでTaskの最初に戻す」機能を将来実装する（**現時点では未実装**）。

### 4.4 動作モード選択

アプリ起動時に以下のモードを選択する：

| モード | LSL | フロー | 操作 |
|-------|-----|--------|------|
| **本番モード** | あり | 完全実験フロー（TaskB→TaskA） | 左手ハンドサイン |
| **テストモード** | なし（デバッグログ出力） | TaskB・TaskAの動作確認のみ | 左手ハンドサイン |

テストモードでは LSL マーカーをデバッグログに出力し、sEMG 同期なしで VR 視覚フィードバックのみを確認できる。

> **同意・SPQのアナログ化：** 同意取得とSPQ（統合失調型パーソナリティ質問票）の記入は、VR内UIではなく**VR外でアナログ（紙またはオンライン）に実施**する。

> **スキップ機構：** 各教示・VHI誘導・休憩・練習の各パネルに**スキップ機構**を設け、開発・テスト時には待機せず次フェーズへ即時遷移できるようにする。本番モードでの取り扱いは運用時に設定可能とする。

### 4.5 試行間独立性の確保

- **ブロック間リセット：** 各ブロック終了後に30秒の手指運動（握り開き）を実施
- **練習の配置：** Task A の練習ブロックは廃止する。Task B は本計測前に練習ブロック（`practiceTrialCount`、調整可能・初期値3試行）を実施し、ペース化屈曲の操作と遅延体験に慣れさせる。練習試行は解析対象外とする。
- **タスク順序（v5.3 変更）：** **Task B → Task A** の順序とする。Task B のコントロール条件を「VHI誘導なし」で構成する都合上、VHIをまだ体験していない状態でasyncブロックを先に行う（詳細は6節・限界は12節）。

### 4.6 随意/不随意EMGの弁別設計

随意運動と不随意motor overflowを区別するために、以下の3層の担保を設ける。

**Layer 1（教示と自己申告）**
「手を完全にリラックスさせ、絶対に動かさないでください」と教示。動いてしまったと感じた試行は被験者が申告し除外する。

**Layer 2（時間窓による弁別）**
```
t = 0ms（バーチャルハンド動作開始）
│
├── t = 0〜100ms ：不随意窓（motor overflow評価対象）
│   随意反応時間（≥150ms）より短く、意図的追随は不可能
├── t = 100〜150ms ：グレーゾーン（解析から除外）
└── t = 150ms〜 ：随意窓（随意的追随として除外）
```

**Layer 3（コントロール差分）**

$$\text{VHI特異的motor overflow} = \text{EMG}_{\text{sync}} - \text{EMG}_{\text{async}}$$

sync条件（VHI成立）とasync条件（VHI不成立）の差分を取ることで、単なる視覚追随の随意成分を相殺し、VHI特異的な不随意成分のみを抽出する。

---

## 5. VHI誘導プロトコル

VHI誘導はタスクの目的（受動的SoO vs 能動的SoA）に応じて異なる構成を採用する。各Phaseにはスキップ機構を設け、開発・テスト時は即時遷移を可能とする（4.4参照）。

> **VAS廃止（v5.3）：** VHI成立を確認するVAS（主観評価）は、個人差が大きく品質管理指標として不適と判断し全廃する。これに伴う研究設計上の限界は12節に明記する。

### Task B 向け誘導（能動的SoA最大化）※sync条件でのみ実施

```
Phase 1（1分）：視触覚同期刺激（筆なぞり）
① 実験者が筆で被験者の左手をなぞる
② Unityがバーチャルハンドにも同期して「なぞられる」映像を提示
③ 被験者は静止

Phase 2（1分）：能動的慣らし運動
① Δt=0ms（遅延なし）の状態でQuestが手を完全に1:1追従
② 被験者がゆっくり左手首を数回屈曲
③「バーチャルハンドが完全に自分の手として動く」体験を強化

Phase 3（30秒）：安静・ベースラインEMG確立

↓ Task B（sync）本計測開始
```

> **async条件（コントロール）には誘導を行わない（v5.3）：** async条件では上記のVHI誘導を一切実施せず、素の状態で Task B を行う。これによりVHIが成立していない状態をコントロールとする。

### Task A 向け誘導（受動的SoO最大化）

```
Phase 1（2分）：視触覚同期刺激（筆なぞり）
① 実験者が筆で被験者の左手をなぞる
② Unityがバーチャルハンドにも同期して「なぞられる」映像を提示
③ 被験者は完全静止・リラックス（随意運動なし）

Phase 2（30秒）：安静・ベースラインEMG確立
① 筆なぞりを完全停止
② 「手をリラックスさせてください」と教示
③ このフェーズのEMGをベースラインとして記録

↓ Task A 計測開始
```

---

## 6. 実験課題

> **実施順序（v5.3）：** Task B → Task A の順に実施する。Task B 内では async（誘導なし）→ sync（誘導あり）の順に行う。

### Task B｜SoAの客観的定量化（運動準備EMG × 遅延操作）

**目的：** 被験者の随意運動（左手首屈曲）に映像遅延Δtを挿入し、Δtを変化させることで運動準備EMGパターン（t=−500〜0ms窓）がどのように変容するか、およびSoA崩壊閾値τ_SoAを測定する。

#### 条件設計（v5.3）

| 条件 | VHI誘導 | 位置づけ |
|------|---------|---------|
| **async（コントロール）** | なし | VHI未成立状態。最初に実施 |
| **sync** | あり | VHI成立状態。asyncの後に実施 |

VHI特異的なSoA指標は sync と async の差分として抽出する。

#### 試行方式

1試行に**1つの固定遅延 Δt** を与える（QUEST法）。1試行は「計測フェーズ」と「回答フェーズ」に時間的に分離する。

**計測フェーズ（EMG記録対象）**

```
① 試行開始の合図（視覚＋音）
② 被験者は「5秒に1回」のペース合図（視覚＋音、pacingInterval＝調整可能・初期値5秒）に
   合わせて左手首を1回ずつ屈曲する
③ 各屈曲に対し、この試行の固定遅延 Δt でバーチャルハンドを描画
④ プログラムが屈曲を検出してカウントし、flexionCountPerTrial（調整可能・初期値3回）に
   達するまで継続
   ※ カウントは被験者ではなくプログラムが「検出ベース」で行う（被験者は数えない）
⑤ 規定回数に達したら計測フェーズを終了
```

**回答フェーズ（EMG計測対象外）**

```
⑥ 「回答時間」の合図（視覚＋音）を提示し、回答フェーズに入ったことを被験者に明示する
⑦ 直前の計測中に「自分が動かしている感じがしない（SoA崩壊）」を感じていれば
   → 左手の所定ハンドサインで申告（＝Yes）
⑧ 何も感じなければ → 応答なし（無反応＝No）
⑨ QUEST法でΔtを更新し、次の試行へ
```

- 遅延範囲：0〜600ms
- QUEST法の初期推定：τ=300ms、傾きβ=2.0

> **方式の根拠：** ①Onset検出からのリングバッファ遅延は設計が複雑なため廃止し、固定遅延を直接適用する。②自由な連続運動はEMGの運動準備窓（t=−500〜0ms）に直前運動の余波が混入し解析を困難にするため、「5秒に1回・規定回数」のペース化運動とし、各屈曲を静止区間で分離する。③報告動作のEMG混入を避けるため、ハンドサイン申告は計測対象外の回答フェーズに限定する。④回答フェーズの開始を合図で明示することで「無反応＝No」の取りこぼし（報告し忘れ）を抑制する。

#### SoA崩壊申告プロトコル

各試行の**回答フェーズ**で被験者は以下の方法で応答する：

| 応答 | 方法 | 意味 | QUEST法への入力 |
|------|------|------|--------------|
| **Yes（崩壊）** | **左手ピンチ（親指+人差し指の先端を合わせる）** | 「自分が動かしている感じがしない」 | Δtを短縮方向に調整 |
| **No（維持）** | 無反応（規定時間内にサインなし） | 「まだ自分が動かしている感じがある」 | Δtを延長方向に調整 |

- 報告は計測対象外の回答フェーズで行うため、報告動作の筋活動は測定信号に混入しない。
- 「無反応＝No」を確実に区別するため、回答フェーズ開始の合図を必須とする。

応答タイムスタンプをLSLマーカーで記録（無反応Noも一律 `SoA_No_...` で記録する）：
- `"SoA_Yes_Trial{n}_Dt{delta_t}ms"`
- `"SoA_No_Trial{n}_Dt{delta_t}ms"`

#### 試行構成

| 内容 | 試行数 |
|------|--------|
| 練習（解析対象外、`practiceTrialCount`・初期値3） | 3 |
| QUEST法による適応的Δt調整 | 15試行 |
| 固定Δt確認試行（0, 150, 300, 500ms 各2試行） | 8試行 |
| 合計（解析対象、1ブロック） | **23試行、約15〜18分** |
| async + sync 合計 | **46試行、約30〜36分** |

> **所要時間の試算：** ペース化屈曲（5秒間隔×初期値3回＝約15秒）と屈曲後待機（3秒）・回答フェーズ（5秒）・試行間インターバル（2〜4秒）を含め、1試行あたり30〜40秒程度を想定。`flexionCountPerTrial` を増やすと比例して伸びるため、本実験前にパイロットで所要時間とSN比のバランスを取って確定する。

#### 得られる指標

| 指標 | 定義 | 意味 |
|------|------|------|
| τ_SoA（EMGベース） | 運動準備EMGパターンのマハラノビス距離が有意変化するΔt閾値 | 主体感崩壊閾値（客観的） |
| PRI（pre-movement readiness index） | t=−500〜0ms窓のEMGパターン一貫性指数 | 運動意図の安定性 |
| SCR（synergy change ratio） | Δt増大に伴う筋シナジー変容率 | 遅延が運動制御に与える影響 |

---

### Task A｜SoOの客観的定量化（motor overflow × 筋シナジー）

**目的：** VHI成立時に、バーチャルハンドの**3指（人差し指・中指・薬指）のランダム自動屈曲**を「見るだけ」で前腕筋に不随意的な筋活動（motor overflow）が生じるかを2ch EMGで定量化する。

#### バーチャルハンドの動作仕様

- **動作内容：** 左手の人差し指・中指・薬指の MCP・PIP・DIP の屈曲（最大30°）。各試行で3指のいずれか1指のみが屈曲する。
- **動作生成：** XRHandSkeletonDriverによるリアルタイムトラッキングを維持したまま、LateUpdate内で屈曲対象指のボーンのみに差分回転を加算（他指は実際の手の形状をリアルタイム反映）
- **開始タイミング：** 速度ベースの静止確認は廃止する。Task A 開始から `autoMotionStartDelay`（調整可能・初期値5秒）の経過後に自動屈曲を開始する。待機中もトラッキングは継続し、手の微細な揺れはVRにリアルタイム反映される（visuoproprioceptive conflictを防ぎSoOを維持するため）。
- **自動屈曲の時間：** `autoMotionDuration`（調整可能・初期値0.5秒）。
- **繰り返し間隔：** 自動屈曲（初期値0.5秒）＋ランダム待機（`autoMotionIntervalMin=5秒` 〜 `autoMotionIntervalMax=10秒` の一様分布）のサイクルを繰り返す。
- **指の順序：** 各指が均等回数（21試行なら各7回ずつ）になるようリスト生成 → Fisher-Yates シャッフルで順序ランダム化。

> **3指に拡張した根拠（A-2 修正）：** 単一指（旧設計：人差し指のみ）だと屈曲指の予測可能性が高く、被験者の予測的注意がEMGに影響する可能性がある。3指をランダム化することで予測不能性を確保しつつ、各指の屈曲回数を均等にすることで指間の比較が可能。屈曲間隔もランダム化することで予測的運動準備のEMGへの混入を抑制する。

#### 試行構成

| ブロック | 条件 | 試行数 | 内容 |
|---------|------|--------|------|
| Block 1 | async（VHI不成立） | 21試行 | 誘導なしで計測。各指7回ずつランダム順序 ※先に実施 |
| Block 2 | sync（VHI成立） | 21試行 | 筆なぞり誘導後に計測。各指7回ずつランダム順序 |
| 合計 | | **42試行** | 約7〜10分（待機5〜10秒×21試行×2ブロック） |

> **Task A の async（v5.3）：** Task A のコントロールも Task B と整合させ、誘導なし条件とする（旧 v5.2 の Δt=500ms 方式は Task B の方針変更に伴い見直す）。

#### 得られる指標

| 指標 | 定義 | 意味 |
|------|------|------|
| MOA（motor overflow amplitude） | t=0〜100ms窓のEMG RMS（sync−async） | SoO強度の客観的指標 |
| MOL（motor overflow latency） | EMGオンセットまでの潜時 | 身体表象の活性化速度 |
| NSS（NMF synergy similarity score） | 人差し指屈曲に対応した筋シナジーとのコサイン類似度 | 運動制御レベルのVHI成立度 |

**NMF筋シナジー解析：**

$$\text{NSS} = \frac{\mathbf{W}_{\text{overflow}} \cdot \mathbf{W}_{\text{voluntary}}}{|\mathbf{W}_{\text{overflow}}||\mathbf{W}_{\text{voluntary}}|}$$

---

## 7. 実験全体のフロー

```
事前（実験前日まで・VR外でアナログ実施）
SPQ記入（紙またはオンライン）
EHI記入（利き手確認）

実験当日（総所要時間：約70分）
├── 説明・同意取得・電極装着（10分）
│   同意取得はVR外でアナログ実施
│
├── Task B 練習（約3分、解析対象外）
│   └─ practiceTrialCount=3 試行、Δt=0ms固定。ペース化屈曲と回答動作に慣れる
│
├── ★ async ブロック群（誘導なし、約20〜25分）
│   ├── Task B (async)：23試行、約15〜18分
│   │   ※ 各試行：5秒ペース×3回屈曲→3秒待機→5秒回答フェーズ
│   ├── ブロック間休憩（30秒、手指運動）
│   └── Task A (async)：21試行、約3〜4分
│       ※ 各試行：屈曲（0.5秒）→ランダム待機（5〜10秒）
│
├── ★ sync ブロック群（誘導あり、約25〜30分）
│   ├── ブロック間休憩（30秒）
│   ├── VHI誘導 B（筆なぞり+慣らし屈曲、3分）※各Phaseスキップ可
│   ├── Task B Baseline（30秒）
│   ├── Task B (sync)：23試行、約15〜18分
│   ├── ブロック間休憩（30秒）
│   ├── VHI誘導 A（筆なぞり受動、2〜3分）※各Phaseスキップ可
│   ├── Task A Baseline（30秒）
│   └── Task A (sync)：21試行、約3〜4分
│
└── 終了

合計試行数（解析対象）：88試行（Task B 23×2 + Task A 21×2）
合計時間（電極装着・休憩込み）：約70分
```

> **新フロー（D-1）の根拠：** 当初は「Task B 全（async+sync）→ Task A 全（async+sync）」の順だったが、async/sync 間にVHI誘導が挟まることで誘導効果が両タスクで混ざるリスクがあった。新フローでは async ブロック群を連続実施したのち、sync ブロック群を VHI誘導とともに連続実施することで、async（誘導なし状態）と sync（誘導状態）を明確に分離する。これにより VHI 誘導効果が両タスクに同等の条件で適用される。

---

## 8. 解析方針

### 8.1 個人パラメータの推定

各被験者から以下の7次元パラメータベクトルを推定する：

$$\boldsymbol{\theta} = [\text{MOA},\ \text{MOL},\ \text{NSS},\ \tau_{\text{SoA}},\ \text{PRI},\ \text{SCR},\ \text{SPQ}]$$

### 8.2 EMG前処理パイプライン

```
生EMG信号（左前腕2ch、1000Hz以上）
→ バンドパスフィルタ（20〜450 Hz）
→ 全波整流
→ RMS包絡線（50ms窓）
→ LSLタイムスタンプでUnityイベントと照合
→ 時間窓分割
   不随意窓：t=0〜100ms（Task A motor overflow）
   運動準備窓：t=−500〜0ms（Task B τ_SoA）
→ NMF分解（k=3〜5、繰り返し確認で最適kを選択）
```

> **Task B のエポック切り出し：** ペース化運動により1試行内に複数回（`flexionCountPerTrial` 回、初期値5）の屈曲が含まれる。各屈曲の運動onsetを基準に運動準備窓（t=−500〜0ms）を個別に切り出し、試行内で平均してSN比を高める。5秒間隔で各運動を静止区間により分離しているため、直前運動の余波が準備窓に混入しない。

### 8.3 統計解析

- **SPQスコアとの Spearman順位相関**
- **低SPQ群 vs 高SPQ群** のノンパラメトリック群間検定（Mann-Whitney U）
- **線形混合モデル（LME）** による試行レベルの回帰分析
- **PCA + マハラノビス距離** によるτ_SoAの定義と推定

### 8.4 NMF筋シナジー解析

```
Task Aの計測EMG（2ch × 時間）
↓
NMF分解：V ≈ W × H
├── W（2 × k行列）：筋シナジー基底（空間パターン）
└── H（k × 時間行列）：シナジー係数（時間プロファイル）
↓
W_overflow（VHI条件）vs W_voluntary（随意運動条件）
↓ コサイン類似度 NSS の算出
↓ SPQとの相関分析
```

---

## 9. 計算論的精神医学モデルとの統合

$$
\text{SPQ} \uparrow
\Rightarrow \Pi_{\text{motor}} \downarrow
\begin{cases}
\text{MOA} \uparrow & \text{（視覚→運動の漏れ込み増大）} \\
\tau_{\text{SoA}} \uparrow & \text{（遅延許容の増大）} \\
\text{NSS} \uparrow & \text{（バーチャルハンドとの筋シナジー同期増大）}
\end{cases}
$$

```
統合失調型特性（SPQ↑）
↓
Π_motor（運動予測精度）↓
↓
┌────────────────────────────────────────────┐
│ MOA増大：視覚的身体表象が運動系に漏れ込む │
│ τ_SoA延長：主体感崩壊への抵抗性低下      │
│ NSS増大：バーチャルハンドへの運動系同期  │
│ PRI低下：運動意図の生成が不安定          │
└────────────────────────────────────────────┘
↓
臨床症状（被影響体験・離人感）の連続スペクトラムモデル
```

---

## 10. 本研究の新規性

| 観点 | 従来研究の限界 | 本研究のアプローチ |
|------|-------------|-----------------|
| SoO測定 | Likertアンケートに依存 | **EMG motor overflow**（t=0〜100ms）で客観的定量化 |
| SoA測定 | 主観的アンケートまたはLibet時計（誤差±150ms以上） | **運動準備EMGパターンの変容**で客観的定量化 |
| EMGの役割 | 行為時刻の検出のみ | **SoO・SoAの主要指標**として活用 |
| 筋活動解析 | 単チャンネルの振幅・オンセットのみ | **2ch NMF筋シナジー解析**で空間パターンを定量化 |
| VHI誘導 | 固定プロトコル（画一的） | **タスク目的に応じた誘導（受動的SoO誘導 / 能動的SoA誘導）**を使い分け |
| バーチャルハンド駆動 | EMGオンセット駆動または触覚同期 | **Questハンドトラッキング**による自然な手動作でVHI誘導 |
| システム | PC依存・実験室固定 | **Questスタンドアロン**によるポータブル実験環境 |
| 理論的枠組み | 行動指標の記述にとどまる | **予測符号化モデルのΠ_motorパラメータ**へのEMG指標の埋め込み |

---

## 11. スケジュール（修正版）

```
Week 1-2│ Unity実装・動作確認（統合フェーズ）
        │ ├─ テストモード / 本番モード選択画面
        │ ├─ 操作系：左手ハンドサインへの全面移行（右手・コントローラー非表示）
        │ ├─ Task A：人差し指自動屈曲（時間ベース待機→自動屈曲）
        │ ├─ Task B：QUEST法 + 固定Δt遅延 + ペース化屈曲 + ハンドサイン申告
        │ ├─ 条件設計：async（誘導なし）→ sync（誘導あり）、Task B→Task A順
        │ ├─ LSL同期システム構築・動作確認
        │ └─ パイロット実験（n=3〜5）・各種パラメータ最終調整

Week 3-4│ 本実験データ収集（n=25〜30）
        │ 各被験者：約80分

Week 5  │ 個人パラメータ推定（MOA, τ_SoA, NSS等）
        │ SPQとの相関・群比較
        │ NMF筋シナジー解析

Week 6  │ 予測符号化モデルへの統合・図式化
        │ 発表スライド・原稿作成
```

---

## 12. 研究設計上の限界（v5.3で明記）

本バージョンの設計判断に伴い、以下の限界が生じる。解釈・考察の際に留意し、論文化時には明示する。

1. **コントロール条件と順序効果の交絡：** async（誘導なし）を sync（誘導あり）より先に固定実施するため、「VHI誘導の有無」の効果と「実施順序・学習・課題への慣れ」の効果が分離できない。被験者間カウンターバランスを行っていないため、async と sync の差分にこれらが交絡しうる。

2. **VHI不成立の保証がない：** async を「誘導なし」で定義しているが、これはVHIが確実に不成立であることを保証しない（単に誘導していない状態）。Δt=500ms のような能動的な破壊操作と比べ、コントロールとしての強度は弱い。

3. **VHI成立の品質管理を持たない：** VAS（主観評価）を全廃したため、sync ブロックでVHIが実際に成立したかを確認・除外する手段がない。誘導ありでもVHIが成立しなかった被験者を検出できず、sync−async 差分の解釈に影響しうる。

4. **左手への操作集中：** 計測対象である左手に操作・申告を集中させているため、申告・操作のタイミングを計測対象外に厳密に限定する運用が前提となる。タイミング管理が崩れるとEMGに操作動作が混入するリスクがある。

> これらの限界は既知のものとして受容したうえで本設計を採用する。将来的な改善余地として、コントロール条件への Δt 破壊操作の併用、順序のカウンターバランス、客観指標によるVHI成立チェックの導入が考えられる。

---

## 13. 調整可能パラメータ一覧

実装時に Inspector へ公開し、パイロット実験で最適値を確定する。本実験では確定値で固定する。

| パラメータ名 | 対象 | 初期値 | 内容 |
|------------|------|--------|------|
| `autoMotionStartDelay` | Task A | 5秒（仮） | ブロック最初の試行前のみの待機 |
| `autoMotionDuration` | Task A | 0.5秒（仮） | 自動屈曲動作の時間 |
| `autoMotionIntervalMin` | Task A | 5秒 | A-2: 自動屈曲後のランダム待機の最小値 |
| `autoMotionIntervalMax` | Task A | 10秒 | A-2: 自動屈曲後のランダム待機の最大値 |
| `trialsPerBlock` (Task A) | Task A | 21試行 | A-2: 各指7回ずつ均等 |
| `pacingInterval` | Task B | 5秒 | ペース合図の間隔（屈曲1回ごと） |
| `flexionCountPerTrial` | Task B | 3回 | 1試行あたりの屈曲回数（検出ベースでカウント） |
| `postFlexionDelaySeconds` | Task B | 3秒 | B-3: 最後の屈曲検出→回答フェーズ開始の待機 |
| `practiceTrialCount` | Task B | 3試行 | 本計測前の練習試行数（解析対象外） |
| `postPracticeDelaySeconds` | Task B | 3秒 | B-1: 練習完了→TaskB_Main 遷移までの待機 |
| `questTrialsCount` | Task B | 15試行 | B-2: QUEST法の適応的試行数（旧 35試行） |
| `totalTrials` (Task B) | Task B | 23試行/ブロック | B-2: QUEST 15 + 固定 8（4Δt×2） |

> 注：「仮」と付した初期値はパイロット実験（Week 1-2）で被験者負担・所要時間・EMGのSN比を踏まえて確定する。`flexionCountPerTrial` は所要時間に直接効くため特に慎重に決める。

---

## 14. 関連文献

- Dempsey-Jones H, Kritikos A (2019). Handedness modulates proprioceptive drift in the rubber hand illusion. *Experimental Brain Research*, 237(2), 351–361.
- Kalckert A, Ehrsson HH (2012). Moving a rubber hand that feels like your own. *Frontiers in Human Neuroscience*, 6, 40.
- Zheng C et al. (2016). Multisensory integration in the virtual hand illusion with active movement. *PLOS ONE*, 11(10), e0165440.
- Shimada S et al. (2016). Robot hand illusion under delayed visual feedback. *PLOS ONE*, 11(7), e0159619.
- Berger CC, Coppi S, Ehrsson HH (2023). Synchronous motor imagery and visual feedback of finger movement elicit the moving rubber hand illusion. *Frontiers in Human Neuroscience*.
- Osumi M et al. (2017). Bodily ownership and motor imagery alter actual muscle activity. *Neuropsychologia*.
- Friston K (2010). The free-energy principle. *Nature Reviews Neuroscience*.

---

## 関連ノート

- [[実験手の選択：右利き者限定・左手の根拠]]
- [[実験操作方式の選択_左手ハンドサインへの全面移行]]
- [[VHI誘導プロトコル_筆なぞりと随意運動の使い分け]]
- [[NMF筋シナジー解析パイプライン]]
- [[EMG-VHI_随意不随意弁別設計]]
- [[システム構成_Questスタンドアロン_LSL_WiFi同期]]
